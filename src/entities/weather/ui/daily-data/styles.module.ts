import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  main: {
    flex: 1,
    marginHorizontal: 'auto',
  },
  chart: {
    borderRadius: 25,
    alignSelf: 'center',
    paddingBottom: 50,
  },
});
